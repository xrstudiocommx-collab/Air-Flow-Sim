## Packages
konva | High performance canvas library for vector drawing layers
react-konva | React bindings for Konva
use-image | Hook for loading images in Konva
d3-scale | Utility for mapping data values to visual colors (heatmap)
d3-interpolate | Smooth color interpolation for heatmaps

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
